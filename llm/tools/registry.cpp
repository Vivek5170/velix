#include "registry.hpp"

namespace velix::llm::tools {

// Intentionally minimal translation unit so the module has an explicit .cpp
// file while retaining header-defined behavior for current build wiring.

} // namespace velix::llm::tools
