{
  "name": "web_search",
  "type": "skill",
  "description": "Searches the web for current info.",
  "runtime": "python",
  "entry": "main.py",
  "workdir": ".",
  "prepare": [],
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search query text"
      }
    },
    "required": ["query"]
  },
  "run": {
    "command": "uv",
    "args": ["run", "main.py"]
  }
}
